<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.kh.petvely.dao.MessageDAO">
	<select id="selectMessages"  resultType="MessageVO">
        select * from message 
        	where
        mes_me_receiverNum = #{mes_num} 
        	order by
        mes_date desc
    </select>
    <select id="selectMemberIds" resultType="MemberVO">
    	select * from member where me_id = #{senderNum}
    </select>
    <insert id="insertMessage" parameterType="MessageVO">
        insert 
        	into
        message 
        	(mes_me_senderNum, mes_me_receiverNum, mes_content, mes_date)
        values 
        	(#{mes_me_senderNum}, #{mes_me_receiverNum},#{mes_content}, NOW())
    </insert>
	
	<select id="selectReceiverId" resultType="Integer">
        select me_num from member where me_id = #{receiverId}
    </select>
	
	<select id="selectsenderId" resultType="Integer">
        select me_num from member where me_id = #{senderId}
    </select>
    
    <select id="selectMarketPostUserId" resultType="Integer">
    	select * from marketpost where po_me_num = #{postUser}
    </select>

</mapper>